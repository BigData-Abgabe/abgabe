To run the code use:
hadoop java random.jar bigdata.TotalOrder <input> <output>

instead of random split1000.jar or split3000.jar can be used woth the same settings

the discussion of the wokbalance is inside the java class